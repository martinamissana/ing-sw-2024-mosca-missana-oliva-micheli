package it.polimi.ingsw.model.deck;

/**
 * Interface that allows to choose a value from DeckType or DeckBufferType.<br>
 * Used to abstract the action of drawing cards with respect to the card's source,
 * making it equivalent to draw from a deck or a deck buffer.
 */
public interface DeckTypeBox {
}
